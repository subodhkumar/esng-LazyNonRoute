import { LazyModuleLoadDirective } from './lazy-module-load.directive';

describe('LazyModuleLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyModuleLoadDirective();
    expect(directive).toBeTruthy();
  });
});
